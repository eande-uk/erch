#!/bin/bash

# erch:summary=Overwrite the user configs for the Walker application launcher (which also powers the erch Menu) and restart the services.

mkdir -p ~/.config/autostart/
cp $ERCH_PATH/default/walker/walker.desktop ~/.config/autostart/

# And restarts if it crashes or is killed
mkdir -p ~/.config/systemd/user/app-walker@autostart.service.d/
cp $ERCH_PATH/default/walker/restart.conf ~/.config/systemd/user/app-walker@autostart.service.d/restart.conf

systemctl --user daemon-reload

# Refresh configs
erch-refresh-config walker/config.toml
erch-refresh-config elephant/calc.toml
erch-refresh-config elephant/desktopapplications.toml

# Link all elephant menus
mkdir -p ~/.config/elephant/menus
for menu in $ERCH_PATH/default/elephant/*.lua; do
  ln -snf "$menu" ~/.config/elephant/menus/"$(basename "$menu")"
done

# Restart service
erch-restart-walker
