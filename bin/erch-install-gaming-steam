#!/bin/bash

# erch:summary=Install Steam and graphics drivers selected for this system
# erch:requires-sudo=true

set -e

echo "Installing Steam..."
erch-pkg-add steam
erch-install-gaming-gpu-lib32

echo ""
echo "Steam will start automatically now. This might take a while..."

setsid gtk-launch steam >/dev/null 2>&1 &
