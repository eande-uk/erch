#!/bin/bash

# erch:summary=Install the ONCE service, enable its background service, and launch the TUI.
# erch:requires-sudo=true

echo "Installing ONCE..."
erch-pkg-add once-bin

echo "Enabling ONCE background service..."
sudo systemctl enable --now once-background.service

echo -e "\nLaunching ONCE..."
once
