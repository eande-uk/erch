#!/bin/bash

# erch:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/erch/toggles/hypr/internal-monitor-disable.conf"

if [[ -f $TOGGLE ]] && ! erch-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
