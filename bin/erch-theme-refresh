#!/bin/bash

# erch:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.config/erch/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  ERCH_THEME_SKIP_BACKGROUND=1 erch-theme-set "$(cat $THEME_NAME_PATH)"
fi
