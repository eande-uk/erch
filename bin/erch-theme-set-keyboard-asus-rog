#!/bin/bash

# erch:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# erch:hidden=true

ASUSCTL_THEME=~/.config/erch/current/theme/keyboard.rgb

if erch-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
