#!/bin/bash

# erch:summary=Show current theme
# erch:examples=erch theme current

THEME_NAME_PATH="$HOME/.config/erch/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  cat $THEME_NAME_PATH | sed -E 's/(^|-)([a-z])/\1\u\2/g; s/-/ /g'
else
  echo "Unknown"
fi
