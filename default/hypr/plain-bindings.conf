# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(erch-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, erch-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(erch-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, erch-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, erch-launch-browser --private
bindd = SUPER SHIFT, N, Editor, exec, erch-launch-editor

# Add extra bindings
# bindd = SUPER SHIFT, A, ChatGPT, exec, erch-launch-webapp "https://chatgpt.com"
# bindd = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting erch Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, erch menu, exec, erch-menu
