# config/walker/ — Walker Application Launcher

Walker app launcher configuration: theme, fuzzy search, keyboard shortcuts, and plugin definitions.

## Contents

| File | Purpose |
|------|---------|
| `config.json` | Launcher settings and keybindings |
| `style.css` | Theme-aware CSS (rendered from `.tpl`) |

## Refresh

```bash
erch refresh walker
```

## Cross-references

- [L3 Theme](../../default/themed/) — `walker.css.tpl` template
- [L1 Defaults](../../default/walker/) — upstream config
