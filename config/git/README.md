# config/git/ — Git Configuration

Git configuration: user identity, aliases, diff tools, and merge behavior. Configured during install via `install/config/git.sh`.

## Contents

| File | Purpose |
|------|---------|
| `config` | Git global config (aliases, diff, merge, pull behavior) |

## Cross-references

- [Install script](../../install/config/git.sh) — sets up git during install
- [Git hooks](../git-hooks/) — pre-commit hooks
- [Lazygit](../lazygit/) — TUI git client
